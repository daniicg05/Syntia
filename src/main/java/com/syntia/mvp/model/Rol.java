package com.syntia.mvp.model;

/**
 * Enum que define los roles de usuario en la plataforma Syntia.
 * <ul>
 *   <li>{@code ADMIN} – Administrador del sistema: gestión de usuarios, supervisión y configuración.</li>
 *   <li>{@code USUARIO} – Usuario final: captura de perfil, proyectos, visualización de recomendaciones.</li>
 * </ul>
 */
public enum Rol {
    ADMIN,
    USUARIO
}

